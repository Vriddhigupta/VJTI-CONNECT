<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.it">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".it_chatroom"></activity>
        <activity android:name=".it_assingnment" />
        <activity android:name=".it_classroom" />
        <activity android:name=".IT_login" />
        <activity android:name=".IT_Grades" />
        <activity android:name=".IT_Syllabus" />
        <activity android:name=".IT_TT" />
        <activity android:name=".ITTY">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>