<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.secondyearcivil">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Upload_ass"></activity>
        <activity android:name=".Class" />
        <activity android:name=".Chatroomsycivil" />
        <activity android:name=".Gradesycivil" />
        <activity android:name=".Syllabus" />
        <activity android:name=".TimeTablesycivil" />
        <activity android:name=".LoginCivil" />
        <activity android:name=".SyCivil">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>